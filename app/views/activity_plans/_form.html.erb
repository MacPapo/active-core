<%= form_with(model: activity_plan) do |form| %>
    <% if activity_plan.errors.any? %>
        <div style="color: red">
            <h2><%= t('global.forms.messages.errors', count: activity_plan.errors.count) %></h2>
            <ul>
                <% activity_plan.errors.each do |error| %>
                    <li><%= error.full_message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="row mb-2">
        <div>
            <%= form.label :plan, class: 'form-label fw-semibold text-secondary' %>
            <%= form.select :plan, @plans, {}, { class: 'form-select' } %>
        </div>
    </div>

    <div class="row mb-2">
        <div class="col">
            <%= form.label :cost, class: 'form-label fw-semibold text-secondary' %>
            <%= form.number_field :cost, class: 'form-control', step: 'any', autofocus: true %>
        </div>

        <div class="col">
            <%= form.label :affiliated_cost, class: 'form-label fw-semibold text-secondary' %>
            <%= form.number_field :affiliated_cost, class: 'form-control' %>
        </div>

        <%= form.hidden_field :activity_id %>
    </div>

    <div class="mt-4">
        <%= form.submit class: 'btn btn-dark text-secondary border border-secondary-subtle fs-4 fw-semibold w-100' %>
    </div>
<% end %>
