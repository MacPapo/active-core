<%= form_with(model: subscription_type) do |form| %>
  <% if subscription_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription_type.errors.count, "error") %> prohibited this subscription_type from being saved:</h2>

      <ul>
        <% subscription_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :desc, style: "display: block" %>
    <%= form.text_area :desc %>
  </div>

  <div>
    <%= form.label :duration, style: "display: block" %>
    <%= form.number_field :duration %>
  </div>

  <div>
    <%= form.label :cost, style: "display: block" %>
    <%= form.text_field :cost %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
